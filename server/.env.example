# Copy to .env and load before runserver/gunicorn (or export in shell).
# django-environ not required — set variables in your process/environment.

# Local development (default if unset)
DJANGO_DEBUG=true

